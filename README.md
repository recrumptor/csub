
# Набор скриптов для работы с VLESS‑подписками

⚠️ **Внимание!**  
Проект создан исключительно для экспериментов и анализа ссылок.  
❌ **Не предназначен для использования в качестве готовой подписки.**

## 📌 Назначение

Данный репозиторий содержит набор скриптов и GitHub Actions workflow для:

- автоматического сбора VLESS‑ссылок из указанных подписок;
- очистки ссылок от дублей и мусора;
- фильтрации по допустимым SNI;
- дополнительной фильтрации по стране.

Обработка выполняется полностью в GitHub Actions — без необходимости запускать что‑либо локально.

## 🚀 Как использовать

### 1️⃣ Форк репозитория

Нажмите **Fork** в правом верхнем углу GitHub и создайте копию репозитория в своём аккаунте.

### 2️⃣ Добавьте свои подписки

В файле:

```
site.txt
```

добавьте ссылки на ваши VLESS‑подписки — по одной на строку.

**Пример:**

```
https://example.com/sub1
https://example.com/sub2
```

## ▶️ Запуск обновления

Доступно два варианта:

### 🔹 Вручную

1. Перейдите во вкладку **Actions**
2. Выберите workflow **Update VLESS Keys**
3. Нажмите **Run workflow**

### 🔹 По расписанию (автоматически)

Откройте файл:

```
.github/workflows/update_keys.yml
```

Отредактируйте блок `schedule` под свои нужды.

**Пример запуска каждые 6 часов:**

```yaml
schedule:
  - cron: "0 */6 * * *"
```

## 📁 Что создаётся в результате

После выполнения workflow в репозитории появятся три файла:

### 1️⃣ `vless_list.txt`

- первичный разбор параметров VLESS‑ссылок;
- удаление явных дублей.

### 2️⃣ `cleaned_links.txt`

Дополнительно выполняется:

- очистка от мусора;
- фильтрация по допустимым SNI (на основе внешнего списка whitelist и словаря `mycdn.txt`).

### 3️⃣ `filtered_links.txt`

Финальный список после:

- дополнительного отсева по стране.


## ⚙️ Настройка фильтра по странам

Откройте файл:

```
countries.py
```

Укажите нужные флаги стран в списке:

```python
ALLOWED_FLAGS = ['🇷🇺', '🇩🇪', '🇫🇮', '🇳🇱']
```
---

## ☁️ Включение загрузки на Яндекс.Диск

Если вы форкнули репозиторий и хотите, чтобы итоговые файлы автоматически загружались на ваш Яндекс.Диск, выполните простую настройку.

### 1️⃣ Получите OAuth‑токен Яндекс.Диска

1. Перейдите на страницу: **https://oauth.yandex.ru/**
2. Нажмите **Создать новое приложение**
3. В поле «Права» включите только один пункт:

```
Яндекс.Диск REST API — Доступ к папке приложения
```

4. Сохраните приложение  
5. Нажмите **Получить токен**  
6. Скопируйте выданный токен вида:

```
y0_AgAAAABBBBBBBBBBBBBBBBBBBBBBBBBBBBB
```

Это и есть ваш ключ доступа.

---

### 2️⃣ Добавьте токен в Secrets репозитория

В GitHub:

1. Откройте вкладку **Settings** вашего форка  
2. Перейдите в **Secrets and variables → Actions**  
3. Нажмите **New repository secret**  
4. Введите имя секрета:

```
YANDEX_DISK_TOKEN
```

5. Вставьте полученный токен в поле **Value**  
6. Сохраните

### 3️⃣ Готово

Теперь шаг:

```
Run upload script
```

в workflow автоматически выполнит `upload.py` и загрузит файл `filtered_links.txt на ваш Яндекс.Диск для доступа в режиме б.списков.

---

## 📜 Примечание

Проект носит исследовательский характер.
Автор не несёт ответственности за использование полученных данных.


